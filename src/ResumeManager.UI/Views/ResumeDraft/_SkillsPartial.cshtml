@model ResumeManager.UI.Models.ResumeDraft.ResumeDraftCreateViewModel

<div class="form-inline">
    <div class="panel-body">
        <div class="row">
            <div class="col-md-12">
                <div class="form-group">
                    <label asp-for="@Model.Skill" class="control-label req"></label>
                    <div class="input-group">
                        <input asp-for="@Model.Skill" id="Skill" />
                        <span asp-validation-for="@Model.Skill" id="skillError" class="text-danger"></span>
                        <button type="button" id="addSkill" class="btn btn-primary">Add Skill</button>
                        
                    </div>
                </div>
            </div>
        </div>
        <hr />
        <div class="col-md-12" id="skillsListContainer"></div>
    </div>
</div>
<script>
var resumeDraftId = @Model.Id;
</script>

@*Handlebars template*@
@*<script id="skills-template" type="text/x-handlebars-template">
    {{#each skills}}
    <div class="row hov">
        <div class="col-md-6">
            <span class="badge">{{this}}</span>
        </div>
        <div class="col-md-6 positionIconRemove">
            <span class="iconRemove" data-value="{{this}}"><i class="fa fa-times text-danger" aria-hidden="true"></i></span>
        </div>
    </div>
    {{/each}}
</script>*@


<script id="skills-template" type="text/x-handlebars-template">
    <table class="table table-hover table-responsive">
        <thead>
            <tr>
                <th>Skill</th>                
                <th></th>
            </tr>
        </thead>
        <tbody>
            {{#each skills}}
            <tr>
                <td>{{name}}</td>                
                <td>                    
                    <button type="button" class="btn removeSkill" data-id="{{id}}"><i class="fa fa-times text-danger fa-lg" aria-hidden="true"></i></button>
                </td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</script>